CREATE TABLE tbl_idatum_materialcontact_details(
    mcd_id SERIAL NOT NULL,
    details varchar(100000),
    activity_type varchar,
    createddatetime timestamp without time zone DEFAULT now(),
    PRIMARY KEY(mcd_id)
);